name: Deploy Infrastructure (AWS)

on:
  push:
    branches: [main]
    paths:
      - 'iac/aws/**'
      - '.github/workflows/infra-aws.yml'
  pull_request:
    paths:
      - 'iac/aws/**'
  workflow_dispatch:
    inputs:
      action:
        description: 'Terraform action (plan/apply/destroy)'
        required: true
        default: 'plan'
        type: choice
        options:
          - plan
          - apply
          - destroy

env:
  AWS_REGION: sa-east-1
  TF_VERSION: 1.10.0

jobs:
  terraform:
    name: Terraform ${{ github.event.inputs.action || 'plan' }}
    runs-on: ubuntu-24.04
    environment: Produção
    defaults:
      run:
        working-directory: iac/aws

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform Backend (S3 + DynamoDB)
        run: |
          # Criar S3 bucket se não existir
          if ! aws s3api head-bucket --bucket promata-terraform-state 2>/dev/null; then
            echo "📦 Criando S3 bucket para Terraform state..."
            aws s3api create-bucket \
              --bucket promata-terraform-state \
              --region ${{ env.AWS_REGION }} \
              --create-bucket-configuration LocationConstraint=${{ env.AWS_REGION }} || true
            
            aws s3api put-bucket-versioning \
              --bucket promata-terraform-state \
              --versioning-configuration Status=Enabled || true
            
            aws s3api put-bucket-encryption \
              --bucket promata-terraform-state \
              --server-side-encryption-configuration '{"Rules":[{"ApplyServerSideEncryptionByDefault":{"SSEAlgorithm":"AES256"}}]}' || true
            
            aws s3api put-public-access-block \
              --bucket promata-terraform-state \
              --public-access-block-configuration "BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true" || true
          else
            echo "✅ S3 bucket já existe"
          fi

          # Criar DynamoDB table se não existir
          if ! aws dynamodb describe-table --table-name promata-terraform-locks 2>/dev/null; then
            echo "🔑 Criando DynamoDB table para locks..."
            aws dynamodb create-table \
              --table-name promata-terraform-locks \
              --attribute-definitions AttributeName=LockID,AttributeType=S \
              --key-schema AttributeName=LockID,KeyType=HASH \
              --billing-mode PAY_PER_REQUEST || true
            
            aws dynamodb wait table-exists --table-name promata-terraform-locks || true
          else
            echo "✅ DynamoDB table já existe"
          fi

      - name: Terraform Format Check
        run: |
          if ! terraform fmt -check -recursive; then
            echo "⚠️ Formatting issues found. Auto-fixing..."
            terraform fmt -recursive
          fi

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Import Existing Resources
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          TF_VAR_domain_name: ${{ vars.DOMAIN_NAME }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
        run: |
          echo "🔄 Checking for existing resources to import..."
          chmod +x ../../scripts/terraform/import-all-existing-resources.sh
          ../../scripts/terraform/import-all-existing-resources.sh --auto || true

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan \
            -var="domain_name=${{ vars.DOMAIN_NAME }}" \
            -var="cloudflare_api_token=${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            -var="cloudflare_zone_id=${{ secrets.CLOUDFLARE_ZONE_ID }}" \
            -out=tfplan \
            -no-color
        env:
          TF_IN_AUTOMATION: true

      - name: Upload Plan
        uses: actions/upload-artifact@v4
        with:
          name: tfplan
          path: iac/aws/tfplan
          retention-days: 7

      - name: Comment PR with Plan
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const output = `#### Terraform Plan 📝

            <details><summary>Show Plan</summary>

            \`\`\`terraform
            ${{ steps.plan.outputs.stdout }}
            \`\`\`

            </details>

            *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

      - name: Terraform Apply
        if: |
          (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
        run: terraform apply -auto-approve tfplan

      - name: Terraform Destroy
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'destroy'
        run: |
          terraform destroy \
            -var="domain_name=${{ vars.DOMAIN_NAME }}" \
            -var="cloudflare_api_token=${{ secrets.CLOUDFLARE_API_TOKEN }}" \
            -var="cloudflare_zone_id=${{ secrets.CLOUDFLARE_ZONE_ID }}" \
            -auto-approve

      - name: Extract Outputs
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        id: outputs
        run: |
          echo "ec2_public_ip=$(terraform output -raw manager_public_ip)" >> $GITHUB_OUTPUT
          echo "s3_bucket=$(terraform output -raw static_assets_bucket)" >> $GITHUB_OUTPUT
          echo "ssh_private_key<<EOF" >> $GITHUB_OUTPUT
          terraform output -raw ssh_private_key >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Deployment Summary
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          echo "## ✅ Infrastructure Deployed Successfully" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 Infrastructure Details" >> $GITHUB_STEP_SUMMARY
          echo "- **Region**: ${{ env.AWS_REGION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **EC2 IP**: \`${{ steps.outputs.outputs.ec2_public_ip }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **S3 Bucket**: \`${{ steps.outputs.outputs.s3_bucket }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Domain**: https://${{ vars.DOMAIN_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔐 Next Steps: Configure GitHub Secrets" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Run these commands to configure deployment secrets:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "# Configure EC2 host" >> $GITHUB_STEP_SUMMARY
          echo "gh secret set EC2_HOST --body '${{ steps.outputs.outputs.ec2_public_ip }}'" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Configure SSH key (run from iac/aws directory after terraform apply)" >> $GITHUB_STEP_SUMMARY
          echo "gh secret set EC2_SSH_KEY < <(terraform output -raw ssh_private_key)" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Or manually add in Settings > Secrets and Variables > Actions" >> $GITHUB_STEP_SUMMARY

      - name: Notify Discord
        if: always() && github.ref == 'refs/heads/main'
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}
          status: ${{ job.status }}
          title: "Infrastructure Deployment (AWS)"
          description: |
            Region: sa-east-1 (São Paulo)
            Action: ${{ github.event.inputs.action || 'apply' }}
          color: 0x00ff00
          username: GitHub Actions
